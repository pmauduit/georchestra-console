# Dev datadir for the local Spring Boot workflow

This datadir is dedicated to the development Docker stack under `docker/dev`.
It is intentionally isolated from `docker/datadir` so local development changes do
not affect the default environment.
